import React from 'react'
import styled from 'styled-components'

import {
    Container,
    Paragraph,
    BeeHeader,
    CenteringContainer,
    BeeLink
} from '../../../styles.edustyles.js'

const AnnualCalendar = () => {
    return (
        <BeeHeader>This will be the montly list of tasks</BeeHeader>
    )
}

export default AnnualCalendar